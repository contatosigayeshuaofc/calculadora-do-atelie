import { describe, expect, it } from "vitest";

describe("Calculadora do Atelie", () => {
  it("starts with the MVP identity in place", () => {
    expect("Calculadora do Atelie").toContain("Atelie");
  });
});
